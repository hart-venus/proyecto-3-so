#include <time.h>

struct FileInfo {
    char filename[256]; // nombre completo del archivo 
    long size; // tamaño del archivo en bytes
    time_t last_modified; // fecha de la última modificación
};

int main() {
    
}